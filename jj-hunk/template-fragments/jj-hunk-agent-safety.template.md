## jj-hunk Agent Safety

- List hunks before selecting them.
- Write specs to files for review.
- Prefer `--spec-file` for split, commit, and squash operations.
- Verify with `jj diff --git` and `jj status`.
- Use `jj undo` immediately after a bad hunk operation.
