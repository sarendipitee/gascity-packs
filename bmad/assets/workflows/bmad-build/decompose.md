Use the vendored BMAD `bmad-create-epics-and-stories` skill at `{{pack_root}}/vendor/bmad-method/skills/bmad-create-epics-and-stories/SKILL.md`.

Create epics and stories from the approved PRD and architecture. Preserve traceability to BMAD artifacts and translate the resulting story set into the build-base decomposition output.

Do not require BMAD to be installed separately; use the vendored skill file and its local templates.
