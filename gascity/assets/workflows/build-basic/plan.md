Use the built-in Gas City design authoring flow.

Create `implementation-plan.md` with the same artifact contract as the `implementation-plan` stage in the GitHub issue fix workflow. Ground the plan in the generated requirements and repository conventions.

Record the implementation plan path on the workflow root bead before closing.
