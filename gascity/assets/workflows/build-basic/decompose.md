Use the built-in Gas City `create-beads` decomposition flow.

Create task beads or a decomposition artifact from the approved requirements and implementation plan. Preserve traceability from each work item back to the relevant acceptance criteria and plan section.

Record the decomposition output on the workflow root bead before closing.
