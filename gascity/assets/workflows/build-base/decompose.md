This is the `build-base` decompose stage. Treat it as a virtual contract that concrete formulas may override.

Convert the approved requirements and plan into executable work units. The decomposition must preserve traceability back to the plan and should avoid implementation choices not already supported by the approved artifacts.

Close this step only after the decomposition artifact or task beads are recorded on the workflow root bead.
